# 更新报告

## 项目

集成化mod管理器 WinUI 3 版

## 当前版本

- 显示版本：`v3.1.3`
- 文件版本：`3.1.3.0`

## 本次更新内容

### 1. 本地压缩包更新

- 新版发布 ZIP 放入现有程序目录后，会在下次启动时自动识别
- 用户确认后，独立更新组件会等待主程序退出，再替换文件并重新启动
- 自动保留 `config.ini` 与 `beta-shell.json` 中的全部本地配置
- 增加压缩包结构、路径安全校验和失败回滚

### 2. 在线分类配置改进

- 新增按仓库配置在线分类的能力
- 可分别填写在线来源、目标游戏和 GameBanana 皮肤分类 ID
- 不同游戏现在可以使用不同的在线分类配置

### 3. 在线分类过滤优化

- 不再要求分类名称必须完全等于 `Skins`
- 只要分类名称中包含 `Skins` 关键词，就会显示在在线列表中
- 更适合不同游戏中存在分类名称变体的情况

### 4. 界面与本地化修复

- 修复浅色模式下导航按钮错误显示深色背景的问题
- 修复设置页更新按钮缺少文字的问题
- 修复英文模式下在线列表刷新和翻页按钮仍显示中文的问题
- 修复浅色模式下快捷键输入框焦点背景显示为深色的问题
- 统一按钮文案刷新逻辑，避免加载结束后恢复为错误语言

### 5. 版本同步

- 应用显示版本更新为 `v3.1.2`
- 文件版本更新为 `3.1.2.0`
- 同步更新 README、CHANGELOG 和使用说明
