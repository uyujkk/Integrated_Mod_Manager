# 更新报告

## 项目

集成化mod管理器 WinUI 3 版

## 当前版本

- 显示版本：`v3.6.1`
- 文件版本：`3.6.1.0`

## 本次更新内容

### v3.6.1 在线角色列表修复

- 优化收藏按钮的大小、颜色和位置，并为长角色名保留独立显示空间。
- 调整角色卡片与滚动滑块之间的距离，避免相互覆盖。
- 修复终末地在线 Mod 下载量不显示的问题，统一使用现有预设仓库的下载统计回填、SQLite 缓存和热度重算逻辑。
- 重写不含图片的中英文完整手册与快速使用手册；完整手册发布到 GitHub，快速手册随发布包提供。

### 0. 自动更新与更新回滚

- 可在设置中开启或关闭“启动时检查软件更新”。
- 检测到新版本后先询问用户，再自动下载发布 ZIP、显示进度、安装并重启。
- 支持可选 SHA-256 校验，并继续保留配置、SQLite 数据和更新失败回滚能力。

### 1. 角色收藏与 SQLite 加速

- 在线角色可按仓库收藏，收藏项自动置顶。
- 使用 SQLite 保存 Mod 文件索引、角色目录、在线分页、详情、下载量、版本和收藏数据。
- 保留旧 JSON 缓存作为兼容回退，已有缓存无需重新下载。

### 2. 备份管理

- 备份列表显示创建时间、涉及 Mod、占用空间和手动恢复按钮。
- 固定保留 10 份改为用户可设置的磁盘空间上限，范围为 0.5 至 100 GB。
- 超出上限时从最旧备份开始安全清理，当前操作生成的备份不会被立即删除。

### 3. 诊断与无障碍

- 新增脱敏诊断 ZIP，包含版本、系统、错误日志和配置摘要，不包含隐私路径或凭据。
- 可选择导出到本地，或打开预填的 GitHub Issue，由用户检查并决定是否提交。
- 增加键盘导航、焦点提示、屏幕阅读器名称、响应式排版、高对比度与系统动画设置适配。

### 4. 在线页面排版修复

- 修复详情面板打开后在线 Mod 列表被压缩成窄列并居中的问题。
- 移除依赖旧坐标的固定宽度反馈计算，列表直接使用 SplitView 分配的剩余空间。
- 打开或关闭详情面板后会重新计算角色栏方向与方块卡片列数。

### 1. 配置方案

- 每个仓库可以独立保存、更新、应用和删除配置方案。
- 配置方案记录目标文件夹中当前启用且能够在仓库中识别的 Mod。
- 应用方案只会管理已识别 Mod，未知目录保持不变。

### 2. 冲突检测

- 复制单个 Mod 或应用配置方案前，按相对文件路径检查与其他已安装 Mod 的重复文件。
- 忽略根目录预览图、说明和许可证等常见非运行文件，减少无效提示。
- 检测到冲突时先显示文件与来源 Mod，由用户确认是否继续。

### 3. 安装回滚

- 复制、移除和方案切换前自动建立事务备份。
- 操作失败时自动恢复到变更前状态。
- 成功操作也可通过“撤销最近一次 Mod 变更”手动回滚。
- 安装事务备份改为按用户设置的磁盘空间上限保留。

### 4. 下载任务中心

- 下载和解压过程统一显示为任务，并提供进度与状态信息。
- 进行中的任务支持取消，完成的任务支持打开目录。
- 可一键清理已完成、失败和取消的记录。
- 取消或失败时自动清理不完整的下载文件与临时目录。

## English Summary

- Adds per-repository configuration profiles for complete mod setups.
- Preserves unknown target folders when applying a profile.
- Detects duplicate relative file paths before installation.
- Creates transaction backups for copy, remove, and profile changes, with automatic and manual rollback.
- Adds a download task center with progress, cancellation, open-folder actions, and finished-task cleanup.
