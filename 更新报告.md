# 更新报告

## 项目

集成化mod管理器 WinUI 3 版

## 当前版本

- 显示版本：`v3.4.2`
- 文件版本：`3.4.2.0`

## 本次更新内容

### 1. 请求取消与限流保护

- 切换仓库、角色或重复刷新时，自动取消已经失效的在线列表请求。
- 在线列表保持 5 秒超时保护，超时后停止访问并保留当前内容或显示本地缓存。
- GameBanana 返回 `429` 时进入指数冷却，并优先遵循服务器提供的重试时间。
- 冷却期间不会重复访问接口，避免连续刷新进一步触发限流。

### 2. 列表虚拟化

- 在线列表和方块模式改用 WinUI 虚拟化容器。
- 只为当前可见项目创建卡片和加载图片，离开可视区域后复用容器。
- 保留查看详情、访问网页、下载解压、筛选、排序和翻页功能。

### 3. 缓存状态提示

- 在线状态条新增独立数据来源标记。
- 可显示实时数据、缓存时间、离线缓存、限流冷却、连接失败和访问停止状态。
- 超时或限流时优先使用可用缓存，不再清空已经显示的 Mod。

### 4. 配置备份与恢复

- `config.ini` 和 `beta-shell.json` 改为原子写入，避免写入中断产生半个配置文件。
- 配置按 10 分钟间隔创建轮换备份，每个文件保留最近 10 份。
- 主配置损坏时，应用会自动查找并恢复最近一份有效备份。

### 5. 更新回滚

- 本地更新前会在 `backups/updates` 中建立持久版本备份。
- 最多保留最近 3 次更新备份，并记录目标版本及新增文件清单。
- 新版本在 12 秒内无法启动或启动后立即退出时，自动恢复旧文件并重新启动上一版本。
- 更新成功日志只在新运行时确认稳定启动后写入。

### 6. 在线页稳定性与排版

- 超时后停止同一配置的自动重试，不再循环显示 5 秒超时通知。
- 修复角色筛选动画破坏网格布局坐标，Mod 卡片不再覆盖工具栏。
- 修复右侧内边距遗漏，翻页按钮保持完整、等宽显示。

## English Summary

- Cancels obsolete online requests and adds exponential rate-limit cooldown.
- Virtualizes online list and grid cards to reduce memory and image traffic.
- Shows live, cached, stale, cooldown, failed, and stopped data states.
- Uses atomic configuration writes with rotating backups and automatic recovery.
- Keeps three update backups and automatically rolls back when the updated runtime fails to start.
- Stops automatic reload loops after an online request timeout.
- Keeps filtered mod cards below the search, sort, and pagination toolbar.
- Fixes right-edge clipping and standardizes pagination controls.
