# 集成化mod管理器 v3.5.1 使用说明

## 1. 运行方式

1. 将发布压缩包完整解压到任意文件夹
2. 双击 `ModFolderCopier.exe`
3. 首次运行后先配置：
   - `Mod 仓库`
   - `目标文件夹`
   - `启动器`（可选）
4. 点击 `刷新目录`

## 2. 压缩包导入

- 顶部导入按钮会把压缩包导入到当前选中的第一层文件夹
- 第二层区域也支持拖拽压缩包导入
- 当前支持：`.zip`、`.zipx`、`.7z`、`.rar`、`.tar`、`.tar.gz`、`.tar.bz2`、`.tar.xz`、`.tar.zst`、`.gz`、`.tgz`、`.bz2`、`.xz`、`.zst`、`.cab`

说明：

- `.zip` 使用程序内置解压
- `.tar`、`.gz`、`.tgz`、`.bz2`、`.xz`、`.zst` 及对应 `.tar.*` 使用系统 `tar.exe`
- `.7z`、`.rar`、`.zipx`、`.cab` 使用 7-Zip 解压

## 3. 在线 Mod

- 在线页会根据当前仓库配置读取 GameBanana 条目
- 现在支持按仓库分别配置：
  - 在线来源
  - 目标游戏
  - 皮肤分类 ID
- 不同游戏可以建立不同仓库，分别填写对应的分类 ID
- 在线分类过滤规则已放宽，只要分类名称中包含 `Skins` 就会显示

## 4. 快捷键与预览

- 每个第二层 Mod 都可保存自己的快捷键说明和功能描述
- 支持预览图显示与图片拖拽导入
- 支持为每个 Mod 保存网页链接并快速打开
- 点击预览图可打开查看器，并使用滚轮缩放或拖动查看细节

## 5. 界面设置

- 在“设置”中切换中文/English、浅色/深色主题
- “界面密度”可选择舒适或紧凑，紧凑模式能在同一屏幕显示更多内容
- “减少动态效果”可关闭页面滑入和图片淡入效果
- 程序会自动记住窗口位置、窗口大小和界面密度
- 在线图片会缓存在 `WinUI3\cache\online-images`，程序会自动清理旧缓存

## 6. 配置方案与安装安全

- 在“更新与任务”页面可为当前仓库新建配置方案，方案会记录目标文件夹中当前已启用且能够在仓库中识别的 Mod。
- 可更新、应用或删除方案。应用方案时，程序会安装方案中的已知 Mod，并移除不在方案中的其他已知 Mod。
- 目标文件夹中无法对应到当前仓库的目录不会被配置方案删除。
- 开启“安装前检查文件冲突”后，复制 Mod 或应用方案前会列出与其他已安装 Mod 重复的相对文件路径，由用户确认是否继续。
- 复制、移除和方案切换前会自动建立事务备份；失败时自动恢复，也可点击“撤销最近一次 Mod 变更”手动回滚。
- 安装事务备份位于 `backups\mod-installs`，程序仅保留最近 10 份。

## 7. 下载任务中心

- 在线 Mod 下载后会出现在“更新与任务”页面的下载任务中心。
- 任务中心显示准备、下载、解压、完成、失败或已取消状态及当前进度。
- 进行中的任务可以取消；完成后可以打开目标目录；“清理已完成”可移除结束的任务记录。
- 取消或失败时会清理未完成的压缩包和临时解压目录。

## 8. 当前版本

- 显示版本：`v3.5.1`
- 文件版本：`3.5.1.0`

## 9. 使用本地压缩包更新

1. 从正式发布页面下载更高版本的 `Integrated_Mod_Manager-vX.Y.Z.zip`。
2. 不要解压，把 ZIP 直接放进当前 `ModFolderCopier.exe` 所在文件夹。
3. 启动程序，确认检测到的新版本，然后点击更新。
4. 程序会自动退出、替换文件并重新启动。

更新时会保留 `WinUI3\config.ini` 和 `WinUI3\beta-shell.json`，其中的仓库、路径、语言、主题、快捷键、Mod 链接及在线来源记录不会被覆盖。请勿修改发布 ZIP 的文件名；普通 Mod 压缩包不会被识别为软件更新。

应用会在 `backups\config` 轮换保存配置备份，并在配置损坏时自动恢复。软件更新前还会在 `backups\updates` 保留最近 3 个版本的文件；如果新版本无法正常启动，更新代理会自动恢复上一版本并重新启动程序。
