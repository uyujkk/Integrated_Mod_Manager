# 跨 Mod 内部状态预设测试版

> [!CAUTION]
> 这是历史实验记录，不是当前推荐方案。后续验证发现：F10 可能先把游戏内旧状态写回 `d3dx_user.ini`，再执行配置重载，从而覆盖刚恢复的快照；因此本方案没有证明能够可靠恢复任意 Mod 的内部状态。

这是基于 Integrated Mod Manager v3.8.0 制作的独立 `v3.8.1` 热注入实验副本，不会替换正式源码或正式发布版。窗口标题和版本徽章会显示 `HOT INJECTION LAB`。

## 这版解决什么问题

XXMI / 3DMigoto 会把不同 Mod 的内部状态保存到 `d3dx_user.ini`。本测试版把这个状态文件完整复制为一个独立预设，之后可以原样恢复。

不同 Mod 可以使用完全不同的快捷键、变量名、命名空间和状态格式。程序不会读取或推断快捷键，也不会只挑选几个变量，而是保留整个 `d3dx_user.ini` 文件。

## 使用步骤

1. 先正常运行一次游戏，在 Mod 中切换到需要保存的状态，按 `F10` 或正常退出，让 3DMigoto 写入 `d3dx_user.ini`。
2. 完全关闭游戏、XXMI Launcher 和相关加载器。
3. 打开本测试版，进入“Mod 更新”页面。
4. 在“跨 Mod 内部状态预设”卡片中点击“自动定位”；找不到时点击“选择文件”，手动选择 `d3dx_user.ini`。
5. 点击“保存当前状态”，输入预设名称。
6. 需要切换时保持游戏运行，选择预设并点击“热注入并重载”。
7. 确认后立即在 3 秒倒计时内切回游戏；程序会向前台游戏窗口发送 `F10`，请求 3DMigoto 重新载入状态。若没有自动发送，请在游戏中手动按一次 `F10`。

## 安全限制

- 每个命名预设都保存一份完整、原始字节不变的 `d3dx_user.ini` 快照。
- 快照具有文件大小和 SHA-256 校验，损坏或被修改后会拒绝恢复。
- 恢复是整文件恢复，因此会同时恢复当时所有 Mod 的内部状态。
- 热注入允许游戏和 XXMI 保持运行，不会把各个 Mod 自己的功能键当成状态标识。
- 每次注入前都会把原文件备份到 `WinUI3/backups/mod-states`；写入失败时会尝试立即回滚。
- 写入使用同目录临时文件替换，失败时不会把半份内容写进原文件。
- 注入后使用的是 3DMigoto 默认配置重载键 `F10`，它不等于任何 Mod 自己的功能快捷键。

## 当前限制

- 这是首个测试实现，需要确认所使用的 XXMI 分支确实把需要的状态写入 `d3dx_user.ini`。
- 热注入会覆盖当前 `d3dx_user.ini` 的全部内容；如果所使用的 XXMI 分支不支持通过 `F10` 重新读取该文件，可能仍需重新进入游戏。
- 程序不执行按键，也不修改各个 Mod 自己的 INI；它只完整保存和恢复 3DMigoto 的用户状态文件。
- 请先备份实际使用环境，并从少量 Mod 开始测试。
